import { createTheme } from "@material-ui/core";

// custom theme
const theme = createTheme({
  palette: {
    primary: {
      main: "#556cd6",
    },
    background: {
      default: "#fdfdfd",
    },
  },
});

export default theme;
